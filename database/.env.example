# MongoDB Atlas connection string
# Replace <username>, <password>, and <cluster> with your Atlas credentials
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/sigmapay?retryWrites=true&w=majority

# Database name
DB_NAME=sigmapay
